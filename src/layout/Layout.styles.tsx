import styled from "styled-components";

const StyledLayout = styled.div`
    main {
        margin-left: 10vw;
        margin-right: 10vw;
        padding: 0 0.5rem;
    }
`

export default StyledLayout;